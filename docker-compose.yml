services:
  portal:
    build: .
    ports:
      - "8080:80"
    volumes:
      - portal_data:/data
    environment:
      - DB_PATH=/data/portal.db
      - AUTH_USER=admin
      - AUTH_PASS=changeme
      - LOG_LEVEL=info
    restart: unless-stopped

volumes:
  portal_data:
